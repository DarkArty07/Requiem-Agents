Requiem project structure: src/ for implementations, tests/ for pytest tests, requiem/ for Necromancer-friendly bridge modules that re-export from src/, shared/ for shared utilities (eval, session_monitor, opencode_client). The Necromancer may create bridge modules (e.g., requiem/csv_tools.py) that inject src/ into sys.path so tests can import from requiem.xxx.
§
Shade of Programming CANNOT execute terminal commands (find, pytest, etc.). When asked to run shell commands, it tries read_file/cat on random paths instead of using the terminal tool. Failed 6 consecutive attempts (2 tasks x 3 rejections each) on trivially simple commands. This is a systemic limitation — delegate code READING/WRITING tasks to the Necromancer, not command-execution tasks. For running tests or shell commands, the user must do it themselves or the Shade needs a different configuration.